{
  "$schema": "https://json-schema.org/schema",
  "$id": "ts#opensearch#mcp-server-connection",
  "title": "ts#opensearch#mcp-server-connection",
  "description": "Connect a ts#mcp-server to a ts#opensearch project",
  "type": "object",
  "properties": {
    "sourceProject": {
      "type": "string",
      "description": "The project containing the ts#mcp-server to connect from"
    },
    "targetProject": {
      "type": "string",
      "description": "The ts#opensearch project to connect to"
    },
    "sourceComponent": {
      "type": "string",
      "description": "The MCP server component name"
    },
    "preferInstallDependencies": {
      "type": "boolean",
      "description": "Whether to prefer installing dependencies after the generator runs. Set to false to defer installing when batching multiple generators (an install still runs if needed so subsequent generators can compute the Nx project graph); install once at the end.",
      "default": true
    }
  },
  "required": ["sourceProject", "targetProject"]
}
